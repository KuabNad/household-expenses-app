# Firebase backend

The backend is Firebase Authentication plus Cloud Firestore.

- `../firebase.rules` contains the deployable access rules.
- `../firebase.json` points the Firebase CLI at those rules.
- No server-side secrets are required by the Expo app.

See `../docs/setup.md` for the exact setup and deployment steps.
